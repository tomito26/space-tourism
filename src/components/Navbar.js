import logo from 'assets'
const  Navbar = () => {
    return(
        <nav>
            <div className="logo">
                <img src={logo} alt="" />
            </div>
        </nav>
    );
};
export default Navbar;